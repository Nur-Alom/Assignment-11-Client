import React from 'react';

const PlaceOrder = (props) => {
    return (
        <div className="my-5">
            <h2>Hello From Place Order</h2>
        </div>
    );
};

export default PlaceOrder;